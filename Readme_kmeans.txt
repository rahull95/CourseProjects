Function to calculate the k-means clustering of a dataframe.

The function cluster_kmeans(df, k) takes in a dataframe df and number of clusters k as the arguments.

It then returns a new dataframe with a single column - the cluster label for each observation, and the SSE from clustering.